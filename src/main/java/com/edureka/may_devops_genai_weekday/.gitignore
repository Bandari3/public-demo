# Compiled class files
*.class

# Package Files
*.jar
*.war
*.ear
*.zip
*.tar.gz
*.rar

# Log files
*.log
logs/ # ignore the entire logs directory

# IDE-specific files

# IntelliJ IDEA
.idea/
*.iml
*.ipr
*.iws
out/ # Output directory for compiled classes

# Eclipse
.classpath
.project
.settings/
.apt_generated/
.factorypath
.springBeans
.sts4-cache/
bin/ # Default output folder for compiled classes

# NetBeans
nbproject/private/
build/
nbbuild/
dist/
nb-configuration.xml
nbactions.xml

# VS Code
.vscode/ # This is tricky. Sometimes you *want* to share launch configs.
         # Consider adding specific files from .vscode/ if needed (e.g., settings.json)
         # Otherwise, ignoring the whole folder is safest for initial setup.

# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties

# Gradle
.gradle/
build/
*.bak # Gradle creates these for some tasks

# SBT (for Scala/Java projects with SBT)
.cache/
.history/
.bloop/
project/project/
project/target/
target/

# Generated by annotation processors
*.apt_generated

# Virtual Machine crash logs
hs_err_pid*

# OS-specific files
.DS_Store # macOS
Thumbs.db # Windows
desktop.ini # Windows

# Temporary files / backup files
*~
*.bak
*.swp
*.tmp
temp/ # temporary directory

# Other common ones
### Intellij - out folder
# Directories created by `idea` command line launcher.
/.idea/
/out/
/gen/
/.idea_modules/

### Eclipse - class and project files
# Eclipse project files
.classpath
.project
.settings/

# STS (Spring Tool Suite)
.sts4-cache/

# Maven specific exclusions in addition to target/
# Maven's `.mvn/wrapper` contains `maven-wrapper.jar` which should be committed
# but other generated files in `.mvn/` should be ignored.
!.mvn/wrapper/maven-wrapper.jar

# Log files - usually ignore all log files by default
*.log

# Test reports / coverage
/test-output/
/surefire-reports/
/jacoco/
/checkstyle-cache/
/findbugsXml.xml
/pmd.xml
/spotbugsXml.xml

# Configuration files that are typically local or contain sensitive info
# Be very careful with these. If you need to share a template, commit the template
# but ignore the actual config file.
*.properties.local
*.yml.local
.env
.env.*
application-local.properties
application-local.yml

# For Spring Boot (if you use it)
*.original